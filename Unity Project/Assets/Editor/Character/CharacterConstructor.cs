using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterConstructor
    {
    /* 'CharacterConstructor' construye la 'CharacterSheet' a través de la información de 'CharacterEditor'.
     * Para ello vuelca todas las fórmulas que se lean de 'CharacterEditor' en una 'FormulaPool'  que está en
     * una clase 'FormulaManager'. Mediante la clase 'CharacterConstructor' decimos en qué orden se ejecutan las fórmulas y
     * vamos actualizando la 'CharacterSheet'.
     * Cada vez que en el juego se produzca un cambio que pueda afectar a un atributo del personaje se repite todo el proce-
     * so de construcción de 'CharacterSheet'.
     */
	}
